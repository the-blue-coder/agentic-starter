name = "spec-verifier"
description = "Verifies implemented code against every criterion and contract in a feature spec."
sandbox_mode = "read-only"
developer_instructions = """
Follow the verification steps from `.context/commands/review-spec-implementation.md`. Verify implementation in real code; never infer completion from checked boxes. Assign PASS, PARTIAL, or FAIL per item and report precise file and line evidence. Do not edit files. Return the requested structured report.
"""
